<UserControl x:Class="Aprogram.UserControlAccount"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Aprogram"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Loaded="UserControl_Loaded">

    <Grid Background="#212121">
        <TextBlock x:Name="accountdata" Style="{StaticResource MaterialDesignTextBlock}" Text="Load account data" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="15"/>
        <Grid>
            <materialDesign:Snackbar x:Name="notification" IsActive="True" Background="Transparent">
                <materialDesign:SnackbarMessage Content="" Foreground="White" ActionContent="Close" ActionClick="SnackbarMessage_ActionClick" />
            </materialDesign:Snackbar>
        </Grid>
    </Grid>
    
</UserControl>
